/**
 * Temporary local customer identity used by the prototype.
 *
 * Replace this with the authenticated customer ID when
 * real authentication is introduced.
 */
export const LOCAL_CUSTOMER_ID =
  "customer-1";